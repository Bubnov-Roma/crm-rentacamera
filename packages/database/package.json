{
  "name": "@rentacamera/database",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "prisma": {
    "schema": "./prisma"
  },
  "scripts": {
    "build": "prisma generate && echo 'Database package built successfully'",
    "dev": "echo 'Database package does not need dev'",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:format": "prisma format",
    "db:seed": "tsx scripts/seed.ts",
    "db:migrate": "prisma migrate dev",
    "db:reset": "prisma migrate reset"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "prisma": "^6.17.1",
    "tsx": "^4.6.2",
    "typescript": "^5.3.3"
  }
}
